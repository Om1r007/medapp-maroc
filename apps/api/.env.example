# Ce fichier est lu par Prisma (qui ne sait pas remonter au .env racine).
# Garde-le synchronisé avec le .env racine pour la valeur de DATABASE_URL.
DATABASE_URL="postgresql://medapp:medapp_dev_password@localhost:5432/medapp_dev?schema=public"
